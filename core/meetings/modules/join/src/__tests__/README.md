# join module — test suite

Tests use the module's pass/throw style (`npx tsx <file>.test.ts`).

- `admission.test.ts` — admission-outcome detector fixtures (gmeet, teams, zoom, jitsi)
- `defaultBotName.test.ts` — default-bot-name env reading
- `humanized.test.ts` — humanized input interaction tests (`src/googlemeet/humanized/`)
- `join.test.ts` — platform join-flow tests
- `modals.test.ts` — Teams modal dialog tests
- `password.test.ts` — Jitsi password prompt tests
